# Shokker Paint Booth V5 — Python dependencies
# Install: pip install -r requirements.txt

Flask>=2.0
flask-cors
numpy
Pillow
scipy

# Optional — Union Jacked footer OCR (pip install pytesseract + install Tesseract OCR for your OS):
# pytesseract>=0.3.10
